# 代码优化主分支交付说明

- run-id：`20260920T223221-code-optimization-main-sync`
- 日期：2026-09-20
- 基线：`main` / `4de5f597573bc2c45b51ba8ab18a279d054339a0`
- 交付范围：O1–O7 优化、O3/O6 返工、组合测试和可复算性能工具
- 发布方式：从基线建立隔离工作树，只纳入本任务路径；原工作区的既有修改继续保留为未提交状态

## 本次进入 main 的行为

1. 生产脚本按 `index.html` 真顺序进行组合启动测试，覆盖 native 脚本缺失、顺序错误及同步句柄/Promise 两种监听契约。
2. 排程结果记账不再触发第二轮无意义原生对账；真实业务变更仍会重新同步。
3. 事项 URL 只允许可解析的 HTTP(S) 绝对地址成为链接；危险或畸形协议按转义纯文本展示，HTML 属性使用属性上下文转义。
4. 提醒台账、截止台账、投递证据和原生排程使用调用内索引，保留重复 ID 的首项匹配语义。
5. 一次保存复用同一份独立快照；权威提交成功后才发布 `committedAlarmItems`。
6. “已看到未完成”折叠时不再生成隐藏卡片；首页内容未变化时保持现有 DOM，签名使用结构化序列化避免用户内容碰撞。
7. 启动阶段合并重复首页渲染，同时保留启动即处理到期事项、深链和分享参数。

## 提交级验证

在从基线创建的隔离工作树中运行完整 `npm test`：

- unit：330
- native：321
- boot-combination：170
- smoke：244
- regressions：715
- 合计：**1780 通过 / 0 失败**

`git diff --cached --check` 通过。完整输出保存在 `docs/reviews/verification-runs/20260920T223221-code-optimization-main-sync/npm-test.log`。

此前脏工作区曾记录 1806 条通过，其中额外 26 条来自本任务开始前已存在、尚未提交的 `test-smoke.js` / `test-regressions.js` 修改；这些修改没有被夹带到本提交。因此，本说明以 1780 条提交级结果作为远程 `main` 的可复现水位。

## 证据边界

2026-09-20 的 Android 候选曾在真机完成覆盖安装、启动、正式包身份和数据恢复检查，但该 APK 来自包含其他未提交 UI 改动的工作区字节，不能作为本提交的逐字节实机证明。本提交的结论是源码与 Node/VM 组合测试 PASS；精确绑定本提交的新 APK/实机回归为 **NOT_PERFORMED**。

实机原始备份、临时调试 APK、含真实事项的截图/JSON、设备标识与大体积日志保留在本机证据目录，没有进入 Git。仓库只发布源码、无用户数据的测试基线和本说明。
