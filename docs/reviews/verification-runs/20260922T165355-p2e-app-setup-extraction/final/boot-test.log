
== A. 生产组合清单从 index.html 推导（未知脚本 / 缺文件 / 顺序 / 加载期依赖） ==
  ✓ index.html 的脚本集合与顺序与契约逐项相符（15 支）
  ✓ 全部脚本形状合法且文件存在
  ✓ app-core.js 是最后一支（它在 IIFE 开头就取全局模块）
  ✓ 全部 4 条加载期依赖在真实生产顺序里都成立（不只是第一条）

== A2. 反向对照：坏清单必须被明确判错（不是「长度对不上」） ==
  ✓ 顺序倒置 ⇒ 报 load-order（真实后果：截止保护只剩 p24 一个阶段）
  ✓ 未知脚本 ⇒ 报 unknown-script
  ✓ 缺失文件 ⇒ 报 missing-file
  ✓ 日历原语被排到解析器之后 ⇒ 报 load-order:lib/parse-cn.js（新声明有牙齿）
  ✓ 日历原语整支缺席 ⇒ 报 missing-load-time-dependency:lib/parse-cn.js
  ✓ app-core 不在最后 ⇒ 同时报 app-core-not-last 与 lib-after-app-core
  ✓ 整份清单里没有 app-core ⇒ 报 missing-app-core
  ✓ 重复加载同一支 ⇒ 报 duplicate-script

== A3. 预缓存与打包链必须覆盖同一份清单（四件套的收口断言） ==
  ✓ sw.js 的 ASSETS 覆盖 index.html 加载的每一支脚本
  ✓ sw.js 的 ASSETS 里没有磁盘上不存在的条目
  ✓ scripts/sync-www.js 会把每支运行时脚本/目录复制进 www/

== A4. test-smoke.js / test-regressions.js 加载了哪些生产脚本（缺口必须是被声明的，不能是隐形的） ==
  ✓ 两套 harness 的加载清单都可从源码推导出来（不是空数组）
  ✓ test-smoke.js 加载的脚本都在生产清单里（没有自创/拼错的脚本名）
  ✓ test-smoke.js 加载了完整生产清单（缺口为空，不再是「有意的缺席」）
  ✓ test-smoke.js 的覆盖判定无问题（每一支生产脚本都真的被加载）
  ✓ test-smoke.js 里 app-core.js 排在最后
  ✓ test-regressions.js 加载的脚本都在生产清单里（没有自创/拼错的脚本名）
  ✓ test-regressions.js 加载了完整生产清单（缺口为空，不再是「有意的缺席」）
  ✓ test-regressions.js 的覆盖判定无问题（每一支生产脚本都真的被加载）
  ✓ test-regressions.js 里 app-core.js 排在最后
  ✓ 反向对照：少加载一支且无声明 ⇒ 报 unaccounted-production-script:lib/native-reminders.js
  ✓ 反向对照：只放一个没有理由的注入声明不算交代，仍然报错
  ✓ MUT-LOAD-DIAG 变异必红：生产 harness 漏载 app-diagnostics.js 时闭合检查点名缺口

== B. 生产组合在隔离上下文里加载同一顺序，导出与 ready 都要成立 ==
  ✓ 生产清单的 15 支脚本全部加载成功
  ✓ 应用 ready() === true（启动链跑到底）
  ✓ 测试钩子导出存在
  ✓ AttentionNativeReminders 是**真实现**（不是 undefined / 不是空对象）
  ✓ AttentionLib 带 Feedback 与 DeliveryEvidence 两套纯逻辑（app-core 的 FeedbackLib/EvidenceLib 来源）
  ✓ 解析器唯一来源：app-core.parseChineseTime === AttentionLib.parseChineseTime（同一函数对象，不是同名副本）
  ✓ 周期推进唯一来源：app-core.nextRepeatTrigger === AttentionLib.nextRepeatTrigger
  ✓ 周期标签唯一来源：app-core.repeatLabel === AttentionLib.repeatLabel
  ✓ 日历原语唯一来源：AttentionLib.DatePrimitives 存在且带两种 nthWeekdayOfNextMonth 语义
  ✓ app-core 暴露的两种 nthWeekdayOfNextMonth 直接来自该命名空间（没有中间副本）
  ✓ 反向对照：抽掉 lib/date-utils.js 后 parse-cn/repeat 求值即失败（不再静默换一套日历算法）
  ✓ 隔离上下文里 window 就是全局对象本身（平台 mock 与模块看到同一个 Capacitor）
  ✓ IDB 后端真的被用上了（证明 app-core 抓到的 Lib 是加载进沙箱的那一份）
  ✓ 反例：去掉 lib/native-reminders.js ⇒ ready() 明确失败（不再缺件硬跑）
  ✓ 反例：失败面板点名 AttentionNativeReminders，并说清后果是「一条提醒都不会响」
  ✓ 反例：去掉 lib/native-reminders.js 后不读库、不起业务定时器 （消灭「能存能改却一条都不排」的界面）
  ✓ 反例：去掉之后 AttentionNativeReminders 确实是 undefined（不是空对象）
  ✓ 反向对照：覆盖判定把它报成 unaccounted-production-script（不是「数量对不上」）
  ✓ 反向对照：app-core 在 lib 之前时不抛错（所以只能靠清单顺序判定拦住）
  ✓ 反向对照：顺序倒置后 app-core 抓到的 Lib 是空的 ⇒ 存储后端没建起来（IDB 一次都没写）
  ✓ 反向对照：证据模块也就位不了 ⇒ 详情页「提醒结果」这一行直接消失
  ✓ 反向对照：顺序判定同时报出 app-core-not-last

== B2. P1 装配闸门：唯一来源矩阵闭合，缺模块时明确失败且不进入业务启动 ==
  ✓ app-core 引用的每个 Lib/DatePrimitives 符号都在必需导出声明表里（矩阵闭合）
  ✓ 声明表本身非空且至少覆盖日期原语与解析器（防止「把表删空」也能过）
  ✓ 反向对照：新增一个未声明的 Lib.brandNewThing 直接调用 ⇒ 闭合判定报出来
  ✓ 守卫引用被豁免（`if (Lib.x) …` 不计入未声明，但必须出现在 guardedRefs 里）
  ✓ 展示实例 API 双向闭合（源码里的 `appUi.<x>` 与契约逐项对齐）
  ✓ 反向对照：源码里多用一个实例 API 而契约没写 ⇒ 报 missingInContract
  ✓ AI 实例 API 双向闭合（源码里的 `ai.<x>` 与契约逐项对齐，内部消费项单列）
  ✓ 反向对照：源码里多用一个 AI 实例 API 而契约没写 ⇒ 报 missingInContract
  ✓ P2-D 诊断实例 API 双向闭合（源码里的 `diagnostics.<x>` 与契约逐项对齐）
  ✓ P2-E 设置实例 API 双向闭合（源码里的 `appSetup.<x>` 与契约逐项对齐）
  ✓ P2-E 反向对照：源码里多用一个设置实例 API 而契约没写 ⇒ 报 missingInContract
  ✓ 反向对照：源码里多用一个诊断实例 API 而契约没写 ⇒ 报 missingInContract
  ✓ 别名归一生效：`feedbackApi` ⇒ `FeedbackLib`，引用落到 `Feedback.*` 这个声明路径上
  ✓ 产品源码**没有歧义别名**（`f` 那种一名两义已消除：反馈用 feedbackApi / 上传用 selectedFile / 返回值用 feedbackResult）
  ✓ 产品源码**没有歧义引用**（扫描器里不存在「证不了归属就跳过」的残留）
  ✓ 依赖矩阵完全闭合：undeclared / ambiguousAliases / ambiguousRefs / problems 四个都为空
  ✓ 覆盖判定的扫描面已含实际依赖路径（NativeReminders / FeedbackLib / EvidenceLib）
  ✓ MUT-A 唯一别名路径：`const feedbackApi = FeedbackLib; feedbackApi.<未声明成员>()` ⇒ 归一成 `Feedback.independentUndeclaredMember` 并进入 undeclared
  ✓ MUT-B 直接命名空间路径：`FeedbackLib.<未声明成员>()` ⇒ 同样进入 undeclared
  ✓ （前置）同名歧义夹具真的改到了两处（否则下面那条是空断言）
  ✓ MUT-C 同名歧义：`ambiguousAliases` 点名 auditAlias，且 `problems` 非空（既有 ambiguous-alias，也有落在它上面的 ambiguous-ref）——「只报告歧义但仍视为闭合」被明确禁止
  ✓ （前置）MUT-D 夹具真的改到了那行真实调用（否则下面那条是空断言）
  ✓ MUT-D 根对象守卫（别名）：`feedbackApi ? feedbackApi.<未声明成员>()` ⇒ 进入 undeclared 且 problems 点名（不许再进 guardedRefs 冒充可选）
  ✓ （前置）MUT-E 夹具真的改到了那行真实调用（否则下面那条是空断言）
  ✓ MUT-E 根对象守卫（直接命名空间）：`FeedbackLib ? FeedbackLib.<未声明成员>() : null` ⇒ 同样进入 undeclared（说明该缺口与别名解析无关，根因是守卫形态被判错）
  ✓ 对照：**成员级**守卫仍算可选（`if (Ns.member)` 形态进 guardedRefs，不进 undeclared）
  ✓ （前置）扫描器变异副本真的加载成功（否则下面两条是空断言）
  ✓ N3 变红：把根对象守卫加回豁免后，MUT-D / MUT-E 的反例**不再被报出来**（退回第四轮之前的行为 ⇒ 正向断言「根守卫下换成员名必须变红」不是恒真的）
  ✓ N3 收口：变异只影响守卫形态，**无守卫**路径照样能抓到（证明变异是外科手术式的）
  ✓ 反向对照：注释里的 Lib.commentOnlyThing 不被误报（剥注释真的生效）
  ✓ 正向：完整生产组合下启动闸门无问题（startupFailure() === null）
  ✓ 正向：完整生产组合下 ready() === true
  ✓ 正向：闸门声明表通过测试钩子可读、且条目数与源码一致
  ✓ 反例：抽掉 lib/parse-cn.js ⇒ ready() 明确失败（false），不是挂起也不是假装成功
  ✓ 反例：startupFailure() 点名是 parseChineseTime 缺失（且说明后果）
  ✓ 反例：未读库/未迁移 ⇒ IDB 一次都没写（不会出现「以为保存了」的界面）
  ✓ 反例：未启动业务定时器 ⇒ 既没有 15 秒心跳、也没有 2 秒闹钟轮询
  ✓ 反例：失败面板真的被插进了首页容器（不是只 console.error）
  ✓ 反例：日志侧也有记录（「有可见面板」与「有可查日志」两件事都成立）
  ✓ 反例：未做持久化状态恢复 ⇒ state.items 仍为空（没有半套数据被用起来）
  ✓ 反例：抽掉 lib/feedback.js ⇒ ready 失败且点名 Feedback
  ✓ 反例：抽掉 lib/storage.js ⇒ ready 失败且点名 createStorage
  ✓ 反例：日历原语被排到解析器之后 ⇒ 求值即报 eval-failed（顺序错的后果可见）
  ✓ 反例：顺序错时闸门同样拦住启动（ready=false，且点名 DatePrimitives 缺失）
  ✓ 反例：顺序错时不读库、不起业务定时器
  ✓ 边界：纯 Web（脚本齐全、无 Capacitor 桥）⇒ ready=true 且闸门无问题
  ✓ 边界：这个场景里 Capacitor 真的不在（证明上一条不是「Android 桥碰巧也过了」）
  ✓ 边界：纯 Web 下原生 JS 模块仍然完整加载（判据是「模块在不在」，不是「桥在不在」）
  ✓ 边界：纯 Web 下 isNativeAndroid() 为 false（模块在、平台不在 ⇒ 走正常降级）
  ✓ 对照：Android 组合里同一支模块报到同一批成员（两个场景的差只该在桥）
  ✓ 反例：抽掉 lib/ui-format.js ⇒ ready 失败且点名 UiFormat
  ✓ 反例：抽掉 lib/ui-format.js 后不读库、不起业务定时器
  ✓ 反例：lib/ui-format.js 排到 lib/date-utils.js 之前 ⇒ 求值即报 eval-failed
  ✓ 反例：顺序错时闸门拦住启动并点名 UiFormat
  ✓ 反例：抽掉 lib/app-ui.js ⇒ ready 失败且点名 AppUi / AppUi.createUi
  ✓ 反例：抽掉 lib/app-ui.js 后不读库、不起业务定时器、不落库
  ✓ 反例：抽掉 lib/app-ui.js 后仍然渲染出可见失败面板（缺的正是 DOM 能力，不能因此静默）
  ✓ 反例：抽掉 lib/app-ui.js 后失败面板里没有可编辑控件
  ✓ 反例：面板上真的写出了「哪一支没起来」（点名 AppUi），不是只 console.error
  ✓ 反例：抽掉 lib/app-ai.js ⇒ ready 失败且点名 AppAi / AppAi.createAppAi
  ✓ 反例：抽掉 lib/app-ai.js 后不读库、不起业务定时器、不落库
  ✓ 反例：抽掉 lib/app-ai.js 后仍然渲染出可见失败面板
  ✓ 反例：面板上真的写出了「哪一支没起来」（点名 AppAi），不是只 console.error
  ✓ 反例：抽掉 lib/app-ai.js 的失败清单**只**点名 AppAi 系（不是「一支没起来」的连带误报）
  ✓ P2-C 反例：抽掉 lib/app-backup.js ⇒ ready 失败且点名 AppBackup / AppBackup.createAppBackup
  ✓ P2-C 反例：抽掉 lib/app-backup.js 后不读库、不起业务定时器、不落库
  ✓ P2-C 反例：抽掉 lib/app-backup.js 后仍然渲染出可见失败面板
  ✓ P2-C 反例：面板上真的写出了「哪一支没起来」（点名 AppBackup），不是只 console.error
  ✓ P2-C 反例：抽掉 lib/app-backup.js 的失败清单**只**点名 AppBackup 系（无连带误报）
  ✓ P2-D 反例：抽掉 lib/app-diagnostics.js ⇒ ready 失败且点名 AppDiagnostics / AppDiagnostics.createAppDiagnostics
  ✓ P2-D 反例：抽掉 lib/app-diagnostics.js 后不读库、不起业务定时器、不落库
  ✓ P2-D 反例：抽掉 lib/app-diagnostics.js 后仍然渲染出可见失败面板
  ✓ P2-D 反例：面板上真的写出了「哪一支没起来」（点名 AppDiagnostics）
  ✓ P2-D 反例：抽掉 lib/app-diagnostics.js 的失败清单**只**点名 AppDiagnostics 系（无连带误报）
  ✓ P2-D 正向：诊断实例真的可用（合同七个成员都在）
  ✓ P2-E 反例：抽掉 lib/app-setup.js ⇒ ready 失败且只点名 AppSetup 系
  ✓ P2-E 反例：抽掉 lib/app-setup.js 后不读库、不排程、不起心跳
  ✓ P2-E 空 AppSetup 命名空间：首次启动点名 AppSetup.createAppSetup 并 ready=false
  ✓ P2-E 空 AppSetup 命名空间：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 工厂返回空壳：首次启动点名 AppSetup.bind 并 ready=false
  ✓ P2-E 工厂返回空壳：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 工厂抛错：首次启动点名 AppSetup.createAppSetup() 并 ready=false
  ✓ P2-E 工厂抛错：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 bind：首次启动点名 AppSetup.bind 并 ready=false
  ✓ P2-E 缺实例成员 bind：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 maybePromptAndroidNotify：首次启动点名 AppSetup.maybePromptAndroidNotify 并 ready=false
  ✓ P2-E 缺实例成员 maybePromptAndroidNotify：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 noteFirstRemindSaved：首次启动点名 AppSetup.noteFirstRemindSaved 并 ready=false
  ✓ P2-E 缺实例成员 noteFirstRemindSaved：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 openSetupSheet：首次启动点名 AppSetup.openSetupSheet 并 ready=false
  ✓ P2-E 缺实例成员 openSetupSheet：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 renderSetupEntry：首次启动点名 AppSetup.renderSetupEntry 并 ready=false
  ✓ P2-E 缺实例成员 renderSetupEntry：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 renderSetupSheetBody：首次启动点名 AppSetup.renderSetupSheetBody 并 ready=false
  ✓ P2-E 缺实例成员 renderSetupSheetBody：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 runSetupStep：首次启动点名 AppSetup.runSetupStep 并 ready=false
  ✓ P2-E 缺实例成员 runSetupStep：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 setupEvidenceHtml：首次启动点名 AppSetup.setupEvidenceHtml 并 ready=false
  ✓ P2-E 缺实例成员 setupEvidenceHtml：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 setupStepsContext：首次启动点名 AppSetup.setupStepsContext 并 ready=false
  ✓ P2-E 缺实例成员 setupStepsContext：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 startSetupTestRun：首次启动点名 AppSetup.startSetupTestRun 并 ready=false
  ✓ P2-E 缺实例成员 startSetupTestRun：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 stopSetupTestRun：首次启动点名 AppSetup.stopSetupTestRun 并 ready=false
  ✓ P2-E 缺实例成员 stopSetupTestRun：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 缺实例成员 updateSetupEntry：首次启动点名 AppSetup.updateSetupEntry 并 ready=false
  ✓ P2-E 缺实例成员 updateSetupEntry：fail closed 前不读库、不排程、不起业务心跳
  ✓ P2-E 临时 boot 反例只写 /tmp，工作区 app-setup.js 字节未被改写
  ✓ P2-E 正向：设置实例合同成员全部接入生产组合
  ✓ P2-C 正向：备份实例真的可用 —— 快照带 app/schema/items 且**密钥不进备份**
  ✓ 正向：AI 实例真的可用（判据是调用结果，不是 `typeof ai === 'object'`）
  ✓ 正向：`mergedDraft` 真的合出了 AI 结果，且**没动**提交时冻结的那份草稿
  ✓ 正向：归一真的收窄了模型自创的取值（越权优先级 / 白名单外周期都不会进事务）
  ✓ 正向：正常组合下展示能力实例真的可用（不是「绑定存在」而是「能调用」）

== B3. F02 依赖契约（null / 缺成员 / 空壳实例 / 工厂抛错）与 F03 重试后绑定重建 ==
  ✓ F02 反例：Feedback = null ⇒ 闸门报出来（旧实现 `typeof === 'object'` 会放行）
  ✓ F02：失败时**不覆盖**既有绑定（`f ? … : 兜底` 的分支不会突然变成可达的那条）
  ✓ F02 反例：删掉 Feedback.setupSteps ⇒ 闸门点名该成员（不是等业务跑到才 TypeError）
  ✓ F02 反例：删掉 DeliveryEvidence.normalizeEvidence ⇒ 闸门点名该成员
  ✓ F02 反例：DeliveryEvidence = null ⇒ 闸门报出来
  ✓ F02 反例：AppUi.createUi 返回空壳 ⇒ 闸门逐项点名实例 API（工厂「存在」≠「装上了」）
  ✓ F02：空壳被拦下时**没有**继续读写库（页面不会停在「能输入却存不下来」）
  ✓ F02：空壳被拦下时既有绑定保持原样（不会把已在跑的界面换成点不动的那套）
  ✓ F02 反例：AppUi.createUi 抛异常 ⇒ 闸门报出来（不是让启动链中途炸掉）
  ✓ P2-A 反例：AppAi.createAppAi 返回空壳 ⇒ 闸门逐项点名全部 15 个实例 API
  ✓ P2-A：AI 空壳被拦下时既有绑定保持原样（不会把已在跑的界面换成点了没反应的那套）
  ✓ P2-A 反例：AppAi.createAppAi 抛异常 ⇒ 闸门报出来（不是让启动链中途炸掉）
  ✓ P2-C 反例：AppBackup.createAppBackup 返回空壳 ⇒ 闸门逐项点名全部 4 个实例 API
  ✓ P2-C：备份空壳被拦下时既有绑定保持原样（不会把已在跑的备份实例换成点了没反应的那套）
  ✓ P2-C 反例：AppBackup.createAppBackup 抛异常 ⇒ 闸门报出来（不是让启动链中途炸掉）
  ✓ P2-D 反例：AppDiagnostics.createAppDiagnostics 返回空壳 ⇒ 闸门逐项点名全部 7 个实例 API
  ✓ P2-D：诊断空壳被拦下时既有绑定保持原样
  ✓ P2-D 反例：AppDiagnostics.createAppDiagnostics 抛异常 ⇒ 闸门报出来
  ✓ F02：全部还原后 bindRuntime() 无问题（上面每条反例都不是把环境改坏了）
  ✓ F03：每个绑定路径都在必需声明表里（29 条 —— 绑定不得超出闸门检查的范围，这是「同源」的结构保证）
  ✓ F03①：缺 lib/parse-cn.js 时 ready=false（起点与复验一致）
  ✓ F03①：补载之前不起业务定时器（失败是干净的失败）
  ✓ F03①：补载后点「重试」⇒ ready=true
  ✓ F03①：重试后绑定指向**当前**的 Lib.parseChineseTime（不是求值时抓到的 undefined）
  ✓ F03①：15 秒心跳只装了一次（重试不得重复注册监听 / 计时器）
  ✓ F03①：重试后解析器真的可调用（返回对象带 trigger）
  ✓ F03①：重试后「输入 → 保存」真的落库（不再有「启动成功、保存链路不可用」）
  ✓ F03②：app-core 在最前（lib 还没加载）⇒ ready=false，不出现「启动成功但不可用」
  ✓ F03②：失败时绑定保持**空**（而不是绑上一份 undefined 后来变成「假成功」）
  ✓ （前置）原生成员反例补丁真的改到了源码（`reconcile,` ⇒ `reconcile: undefined,`）
  ✓ F02 反例：删掉 NativeReminders.reconcile ⇒ 闸门点名该成员（不是等对账跑到才 TypeError，也不是静默不排）
  ✓ F01 收口：原生成员级失败同样发生在副作用之前（不读库、不排通知、不排闹钟、不起业务心跳）
  ✓ 对照（防恒真）：同一驱动序列下完整源码真的排起闹钟（证明上面的 0 是「被拦住」，不是「本来就不排」）
  ✓ 对照（防恒真）：健康组合上同一驱动序列可正常执行（探针本身是良构的）
  ✓ F01 收口：失败态下走同一驱动序列，**写不进也排不出**（复验表格那一行的反面：落库与排程两个都必须是 0）
  ✓ （前置）Feedback 成员反例补丁真的改到了导出块（两条 shorthand ⇒ undefined）
  ✓ F02 反例：删掉 Feedback.UNDO_WINDOW_MS / TEST_FEEDBACK ⇒ 闸门逐条点名（number 型条目同样受闸门保护，不是只有 function/object）
  ✓ 收口：这两条失败也发生在副作用之前（不读库、不排程、不起心跳）
  ✓ 对照：`Feedback.TEST_FEEDBACK` 是正常数组时应用照常就绪（这组断言不是「恒不启动」）
  ✓ F02-R3 反例：非数组（{}） ⇒ 启动前被点名（期望是数组契约）
  ✓ F02-R3 反例：非数组（{}） ⇒ 报出来的实际值是「object（不是数组）」（说清坏在哪，不只报类型）
  ✓ F02-R3 收口：非数组（{}） 同其它成员级失败一样发生在副作用之前（不开库、不排程、不起心跳）
  ✓ F02-R3 反例：元素缺 label ⇒ 启动前被点名（期望是数组契约）
  ✓ F02-R3 反例：元素缺 label ⇒ 报出来的实际值是「array[0] 缺少 label」（说清坏在哪，不只报类型）
  ✓ F02-R3 收口：元素缺 label 同其它成员级失败一样发生在副作用之前（不开库、不排程、不起心跳）
  ✓ F02-R3 反例：空清单（[]） ⇒ 启动前被点名（期望是数组契约）
  ✓ F02-R3 反例：空清单（[]） ⇒ 报出来的实际值是「空数组（清单至少要有一项）」（说清坏在哪，不只报类型）
  ✓ F02-R3 收口：空清单（[]） 同其它成员级失败一样发生在副作用之前（不开库、不排程、不起心跳）

== B4. 反向对照：拔掉本轮修复，相应断言必须变红（有分辨力的证明） ==
  ✓ （前置）M1 反例补丁真的改到了 null 判据
  ✓ M1 变红：拿掉 null 判据后，`Feedback` 这一条**不再被点名**（只剩成员在报）⇒ B3 断言「path==='Feedback' && actual==='null'」会变红
  ✓ M1 的对照面：同一注入在**未变异**的 app-core 上会点名 `Feedback`（两条合起来才说明判据有分辨力）
  ✓ （前置）M2 反例补丁真的改到了闸门循环
  ✓ M2 变红：闸门不查原生条目后，缺 lib/native-reminders.js 时**不再有任何装配失败反馈**（`startupFailure()` 为 null）⇒ B2 断言「点名 AttentionNativeReminders」不会恒真
  ✓ M2 补充：缺陷态下业务跑不完（中途抛错）、落库与排程都是 0，而界面仍然没有任何失败反馈 —— 比「照跑」更糟
  ✓ M3 变红：闸门漏掉那一条成员声明后，`reconcile: undefined` **不再被点名**⇒ B3 ⑨ 的成员级断言不会恒真
  ✓ （前置）N4 反例补丁真的把期望类型退回 object
  ✓ N4 变红：`TEST_FEEDBACK` 只按 object 校验时，`{}` **重新被放行**（F02-R3 原缺陷复现：ready 起来后再进那一屏才崩）⇒ ⑪ 的断言不会恒真
  ✓ N4 的对照面：同一注入在**未变异**的 app-core 上会被点名且带数组期望（两面合起来才说明「array 契约」四个字是有牙齿的）
  ✓ （前置）N5 反例补丁真的删掉了元素形状核对
  ✓ N5 变红：删掉元素形状核对后，元素缺 label 的清单**重新被放行**（容器对 ≠ 元素对）⇒ ⑪ 的那条断言不会恒真
  ✓ （前置）N6 反例补丁真的清空了 AI 实例契约
  ✓ N6 变红（静态）：契约清空后，源码里用到的 AI 实例 API 全部变成 missingInContract ⇒ ②·3b 那条双向闭合断言不会恒真
  ✓ N6 变红（运行时）：契约清空后，空壳工厂**重新被放行**（一条 AppAi.* 都不报） ⇒ B3 那条「逐项点名」断言会变红
  ✓ N6 的对照面：同一空壳在**未变异**的 app-core 上被逐项点名（两边合起来才说明判据有分辨力）
  ✓ （前置）N7 反例补丁真的删掉了 AppAi 系的声明（4 条）
  ✓ N7 变红：删掉 AppAi 声明后，静态矩阵立刻报 undeclared（证明这 4 条声明是闭合的一部分，不是注释）
  ✓ N7 的对照面：未变异源码下 AppAi 系的引用全部闭合（problems 为空）
  ✓ （前置）N8 反例补丁真的清空了备份实例契约
  ✓ N8 变红（静态）：契约清空后，源码里用到的备份实例 API 全部变成 missingInContract
  ✓ N8 变红（运行时）：契约清空后，空壳工厂**重新被放行**（一条 AppBackup.* 都不报）
  ✓ N8 的对照面：同一空壳在**未变异**的 app-core 上被逐项点名（两边合起来才说明判据有分辨力）
  ✓ 收口：三条反向编辑都用临时副本，工作区 app-core.js / native-reminders.js 未被改写
  ✓ 收口：M2/M3 的补丁在语法上与原文件只差那一两行（防止「改坏了才变红」）
  ✓ （前置）N9 反例补丁真的清空了诊断实例契约
  ✓ N9 变红（静态）：契约清空后，源码里用到的诊断实例 API 全部变成 missingInContract ⇒ ②·3c 那条双向闭合断言不会恒真
  ✓ N9 变红（运行时）：契约清空后，空壳工厂**重新被放行**（一条 AppDiagnostics.* 都不报） ⇒ B3 ⑤·6 那条「逐项点名」断言会变红
  ✓ N9 的对照面：同一空壳在**未变异**的 app-core 上被逐项点名（两边合起来才说明判据有分辨力）
  ✓ 收口：N6/N7 的补丁同样只差那几行（N6 改 1 行、N7 删 4 条声明）
  ✓ 收口：N8 的补丁同样只差那几行（把 2 行成员清单压成 1 行 ⇒ 少 1 行）

== B5. P2-C-R：冷启动权威状态保护（三态判定 / 写闸门 / 恢复面板） ==
  ✓ 首启对照：权威读到空且别处无残留 ⇒ 判 empty、照常就绪（不是恒不启动）
  ✓ 首启对照：empty 开放写入（写闸门不是「一律不写」）
  ✓ 首启对照：业务保存照常落库、原生对账照常发生
  ✓ 读失败：判 failed（原因写明是权威读失败 + 镜像也没有）
  ✓ 读失败：写闸门关闭（writesAllowed=false）
  ✓ 读失败：**不读库不写库**（puts 恒为 0，IDB 逐字节未动）
  ✓ 读失败：**不排通知**（动作类型 / 渠道 / 监听 / 排程一个都没发生）
  ✓ 读失败：**不启业务心跳**（没有 15 秒 tick）
  ✓ 读失败：内存空态**没有**被写成镜像，也没有留下待回放凭据
  ✓ 读失败：显示可见的恢复面板（插进首页容器 + 写明没写任何东西）
  ✓ 读失败：面板里只有按钮，没有任何可编辑控件（不留下「能输入但存不下来」的界面）
  ✓ 读失败：绕过启动链的提交同样被拦（state-authority-blocked，落盘痕迹仍然为 0）
  ✓ 重试成功：判 loaded、原因是从权威读到的（不是镜像兜底）
  ✓ 重试成功：旧数据真的回来了（1 条「既有事项」），IDB 仍然完好
  ✓ 重试成功：面板撤掉、业务启动真的跑起来了（心跳 + 原生对账就位）
  ✓ 打不开：IDB 存在却用不上时，绝不判 empty（否则等于把「读不到」当「不存在」）
  ✓ 打不开：这次启动同样一笔没写（IDB 里那份 1/1 逐字节未动）
  ✓ 打不开：重试会**重新尝试打开权威后端**（故 loaded 的原因必须是权威，而非 degraded-mirror）
  ✓ 打不开：重试真的把那份 1/1 读了回来
  ✓ 镜像迁移：IDB 读到空但镜像有值 ⇒ 判 loaded，并以镜像内容为准
  ✓ 镜像迁移：那一刻真的写回了权威后端（IDB 里现在有 1/1「既有事项」）
  ✓ 残留证据：权威读到空但镜像有不可解析残留 ⇒ failed（不当首启）
  ✓ 脏值：权威里存的不是对象 ⇒ failed，且一笔都没写
  ✓ 桥故障：注入真的命中（被点名的方法**被调用过**并且拒绝了，不是只改了配置）
  ✓ 桥故障：应用仍然就绪（`initializeNativeReminders` 的 catch 兜住，不把启动挂死）
  ✓ 桥故障：权威判定**不受桥影响**（仍是 loaded/authoritative，writesAllowed=true）
  ✓ 桥故障：旧数据完好（IDB 里仍是那 1/1，没有被空态覆盖）
  ✓ 叠加故障：桥故障 + 权威读失败 ⇒ 判 failed、writesAllowed=false
  ✓ 叠加故障：仍然一笔不写（puts=0）、不留待回放凭据、旧 IDB 逐字节未动
  ✓ 叠加故障：既不排通知也不起业务心跳（失败态的业务旁路整段跳过）
  ✓ 叠加故障：恢复面板照样出现（用户看得到「没恢复成功」，而不是一个空收件箱）
  ✓ （前置）M9 反例补丁真的拔掉了四道闸门（各一处、都命中，且只改了这一行）
  ✓ 对照面：未变异源码下，失败态既拦住了提交也没有留下任何待回放凭据
  ✓ M9 变红：拔掉恢复闸门后，读不到权威数据时应用**照常启动**（ready=true、心跳跑起来）
  ✓ M9 变红：这次提交真的写出了落地痕迹（镜像 + 待回放凭据 = 一份**空基线**快照）
  ✓ M9 核心（拔掉闸门必红）：下一次冷启动把空快照经待回放写回健康的 IDB ⇒ 旧 1/1 被覆盖成 0/0
  ✓ 对照面：同一场景下未变异源码的下一次冷启动，旧 1/1 **完好无损**（两边合起来才说明闸门有分辨力）
  ✓ 收口：反向编辑只用临时副本，工作区 app-core.js 未被改写

== B6. 独立复验缺口：陈旧镜像不得覆盖权威（IDB 两条 / 镜像一条，双启动分叉） ==
  ✓ B6/get：权威 get 失败 + 镜像有值 ⇒ 判 **failed**（镜像≠权威，不能升格）
  ✓ B6/get：失败态**不启业务**（没有 15 秒心跳，也没有业务批量往库里写）
  ✓ B6/get：这次启动对权威后端连碰都没碰（puts=0，IDB 仍是那两条）
  ✓ B6/get：用户硬要保存也被拦下（state-authority-blocked），**没有**留下待回放凭据（下一步覆盖的正是这张授权书）
  ✓ B6/get：内存里留下镜像内容只作**只读查看**（这条 OK 不代表它可以写）
  ✓ B6/get：**下一次健康冷启动仍读到权威那两条** ⇒ 旧镜像没有覆盖掉 `idb-new`
  ✓ B6/open：权威 open 失败 + 镜像有值 ⇒ 判 **failed**（镜像≠权威，不能升格）
  ✓ B6/open：失败态**不启业务**（没有 15 秒心跳，也没有业务批量往库里写）
  ✓ B6/open：这次启动对权威后端连碰都没碰（puts=0，IDB 仍是那两条）
  ✓ B6/open：用户硬要保存也被拦下（state-authority-blocked），**没有**留下待回放凭据（下一步覆盖的正是这张授权书）
  ✓ B6/open：内存里留下镜像内容只作**只读查看**（这条 OK 不代表它可以写）
  ✓ B6/open：**下一次健康冷启动仍读到权威那两条** ⇒ 旧镜像没有覆盖掉 `idb-new`
  ✓ B6/对照：权威读得到时镜像**被忽略**（仍从权威读到两条，且判 loaded/authoritative）
  ✓ B6/对照：权威读到**空**而镜像有值 ⇒ 仍走既有的「迁移进权威」语义（loaded，不被本轮收紧误伤）
  ✓ （前置）M12 补丁三处都命中且行数不变（改的是判定本身，不是删掉代码）
  ✓ M12 变红/get：拔掉防线的 权威 get 失败 ⇒ 旧镜像被提升为可写 loaded
  ✓ M12 变红/get：这次保存真的留下了待回放凭据
  ✓ M12 变红/get：**下一次健康启动用旧快照覆盖权威** ⇒ 两条变一条（`idb-new` 消失）
  ✓ M12 变红/open：拔掉防线的 权威 open 失败 ⇒ 旧镜像被提升为可写 loaded
  ✓ M12 变红/open：这次保存真的留下了待回放凭据
  ✓ M12 变红/open：**下一次健康启动用旧快照覆盖权威** ⇒ 两条变一条（`idb-new` 消失）
  ✓ 收口：反向编辑只用临时副本，工作区 app-core.js 未被改写
  ✓ 启动期提交/对照：第一次载入未迁移状态确实需要迁移（IDB 现在已是规范形态）
  ✓ 启动期提交：**连续第二次冷启动不再产生任何提交**（幂等后 puts=0，迁移不是循环）

== C. 可控 Android 桥：真实初始化与生命周期入口 ==
  ✓ 原生初始化注册了本地通知动作类型（监听注册这一幕真的发生了）
  ✓ 通知渠道被建起来（ensureChannels 走到了）
  ✓ 本地通知监听注册了 `localNotificationActionPerformed` 与 `localNotificationReceived`
  ✓ App 的 appStateChange 监听注册成功（回前台补偿的入口）
  ✓ 启动时至少做过一轮原生对账（forceRebuild 那一次）
  ✓ 15 秒心跳与 2 秒闹钟轮询都装上了

== C2. O2：业务保存 → 恰好一轮对账；**内部记账不再自激下一轮** ==
  ✓ 一次业务保存 = 恰好一轮对账（不是两轮）
  ✓ 首次对账排下了全屏闹钟（关键档首投走闹钟通道）
  ✓ 普通提醒通道也照常排程
  ✓ 排程台账被记进已提交快照（业务侧真的收到了结果）
  ✓ 提醒三态台账登记了这一轮的键，且都带有限值的 roundBase
  ✓ 轮次身份是**同一个**（= 登记时事项的触发起点），不是每条各自的 at
  ✓ 后续追提醒的 at 已经推后，但 roundBase 仍是那一轮的起点（R-F06 的判据）
  ✓ 静置 500ms 期间**没有**新增对账（内部记账不再自激下一轮）
  ✓ 静置期间也没有新增桥调用

== C3. 相同计划再对账：允许一轮对账，但不许重排 ==
  ✓ 第二次保存（计划未变）确实又对账了一轮
  ✓ 同一计划的第二轮**不重排全屏闹钟**（签名差量生效）
  ✓ 同一计划的第二轮也不重排普通提醒
  ✓ 第二轮不误撤闹钟

== C4. 改期：旧排程撤销 + 新排程落下 ==
  ✓ 改期后旧闹钟被撤销（不留下幽灵闹钟）
  ✓ 改期后按新时刻重新排下闹钟
  ✓ 新排程用的是**新**时刻（业务改变后最终计划正确）

== C5. 失败保留重试：排程失败不入账，桥恢复后下一轮补排（不谎报已排） ==
  ✓ 排程失败的那一条**没有**被记进排程台账（不入账才可能重试）
  ✓ 失败时既有的那条闹钟仍被保留（不因另一次失败把它忘掉）
  ✓ 失败没有让对账整体崩掉（异常被收进 status，没有逃逸出 API）
  ✓ 桥恢复后下一轮把那条补排上（失败保留重试闭环）
  ✓ 补排真的调到了桥（不是只改台账）

== C6. addListener 两种返回契约（真机同步句柄 / 官方 Promise）都要能启动 ==
  ✓ 同步句柄契约下 ready() === true（init 未被同步异常打断）
  ✓ 同步句柄契约下监听确实注册过
  ✓ Promise 契约下 ready() === true
  ✓ Promise 契约下监听确实注册过
  ✓ 两种契约下都真的排起了原生链路（不是「没抛错」这么弱的判据）

== C7. O5：正常保存的序列化计数（同一份独立快照给两个消费者） ==
  ✓ 一次保存只有 **1 次**全量 JSON.parse
  ✓ 两次 stringify 各自都有出处：① 生成 json 载荷 ② localStorage 镜像序列化
  ✓ 镜像仍然照写（不能为了少一次 parse 把镜像去掉）
  ✓ 权威后端（IDB）上确实落到了这次提交
  ✓ （前置）提交确实停在半路（put 已发出，事务还没完成）
  ✓ 落库的是**提交时刻**的独立快照（在途改内存没混进去）
  ✓ 在途新增的事项没有被塞进这次提交（不是把活数组直接交给后端）

== C8. O5：提交失败不得提前发布已提交快照（旧投递不被误停） ==
  ✓ 权威提交失败被如实上报（不是静默成功）
  ✓ 失败时权威后端上没有写入这次内容
  ✓ 未提交的终态没有被当成已提交 ⇒ 旧投递**没有**被误停
  ✓ 面板把这条在途投递作为「待处理」展示出来
  ✓ 提交成功后已提交快照才更新 ⇒ 旧投递被停掉
  ✓ 真实生命周期入口 visibilitychange 也被接上了（不是只有桥回调）

== D. O7：默认空首页一次启动只做**一次**必要的首页 DOM 构建 ==
  ✓ 空启动：#homeDue 只被重写 1 次
  ✓ 空启动：#homeActive / #homeEmpty / #homeStart 各只被重写 1 次
  ✓ 空首页确实是安静态（空态文案 + 「记一件事」入口）
  ✓ 末尾 tick 仍然执行（心跳与轮询都装上了）
  ✓ 持久数据启动 ready() 为真，且老数据被恢复出来
  ✓ 到期事项在启动当期就被推进为 due（没有等到 15 秒后的那一拍）
  ✓ 持久数据启动：#homeDue 只被重写 1 次
  ✓ 持久数据启动：那张到期卡片就在这一次构建里渲染出来（data-id 已在 DOM 里）
  ✓ 持久数据启动：#homeActive / #homeEmpty / #homeStart 也各只被重写 1 次

== D2. O6：1000 条「已看到未完成」折叠时生成 0 张隐藏卡片 ==
  ✓ 折叠态 #homeActive 里一张 `<article>` 都没有
  ✓ 折叠态仍然给出数量入口（数量、文案、展开动作都在）
  ✓ 折叠态 HTML 体积与卡片数无关（148 字节量级，而不是 600KB 量级）
  ✓ 渲染计数佐证：折叠态生成的 active 卡片数 = 0
  ✓ 展开后按当前状态生成全部 1000 张卡片
  ✓ 展开后没有 hidden 属性（卡片真的可见）
  ✓ 收起后这些卡片节点被释放（#homeActive 里重新变成 0 张）

== E. O3：危险协议没有可点击入口；含引号 ID 不突破属性（走持久化 → normalizeItem → 渲染真实路径） ==
  ✓ 13 条带链接的事项都进入「需要注意」并渲染出来（对照有效）
  ✓ 危险协议一次都没进 href（大小写 / 前导空白 / 内嵌换行 / data / vbscript / file 全覆盖）
  ✓ 所有可点击 href 都是 https:（白名单以外一个都不放行）
  ✓ 危险或畸形链接没有生成可点击的 <a class="linkish">（11 条都不行）
  ✓ 危险或畸形链接以纯文本节点呈现（linkish-plain 计 11 个）
  ✓ 解析器明确拒绝的畸形 IPv6 / percent encoding / 端口没有被兼容正则二次放行
  ✓ 纯文本仍然是**转义后**的（data: 那一条的 <script> 变成了 &lt;script&gt;）
  ✓ 危险值仍然原样留在 it.url 里（降级呈现 ≠ 删用户内容）
  ✓ 正常 HTTPS 仍然可点击，且 href 里的 & 被转义成 &amp;
  ✓ 含引号的 ID 在 data-id 里被转义（裸引号不能出现，否则能闭合属性）
  ✓ 含引号的 ID 没有在 HTML 里留下**裸**的 onmouseover=" 属性
  ✓ 含引号的 ID 也没有以原始形态出现在任何位置
  ✓ 含引号的 ID 在动作按钮上也同样被转义
  ✓ 含引号的 ID 本身没有被改写（不顺带重编号、不删除重复 ID）

== F. O4：100/500/2000 项 × 空台账 / 满台账，不再逐目标整表扫描（含优化前后对照） ==
  ✓ n=100 空台账：**0 次**对大数组（≥64）的 find（整表扫描已消除）
  ✓ n=100 空台账：也没有任何变化需要落库（没有借性能改动顺手改语义）
  ✓ n=100 满台账：同样 **0 次**大数组 find（索引建一次、查 n 次）
  ✓ n=100 满台账：业务结果正确（100 条都登记成当前轮的 scheduled）
  ✓ n=500 空台账：**0 次**对大数组（≥64）的 find（整表扫描已消除）
  ✓ n=500 空台账：也没有任何变化需要落库（没有借性能改动顺手改语义）
  ✓ n=500 满台账：同样 **0 次**大数组 find（索引建一次、查 n 次）
  ✓ n=500 满台账：业务结果正确（500 条都登记成当前轮的 scheduled）
  ✓ n=2000 空台账：**0 次**对大数组（≥64）的 find（整表扫描已消除）
  ✓ n=2000 空台账：也没有任何变化需要落库（没有借性能改动顺手改语义）
  ✓ n=2000 满台账：同样 **0 次**大数组 find（索引建一次、查 n 次）
  ✓ n=2000 满台账：业务结果正确（2000 条都登记成当前轮的 scheduled）
  ✓ 对照（编辑前 app-core.js，n=2000 空台账）：确实在做逐目标整表扫描
  ✓ 对照（本轮 app-core.js，n=2000 空台账）：比较次数降到 0
  ✓ 对照结论可复算：编辑前 ≈ n²/2 量级，本轮为 0（同一数据、同一调用）
  ✓ 对照两侧的业务结果一致（都判定为「无变化」）

== F2. O4：重复 ID 首项胜、cancelled / delivered / suppressed / roundBase 语义逐条保留 ==
  ✓ 重复 ID：登记落在**首项**上（首项胜语义没被索引改成末项覆盖）
  ✓ delivered 是终态：后续对账不抹掉真实送达证据
  ✓ suppressed 是终态：撤销完成时的抑制不会被对账抹掉
  ✓ cancelled：原生确认撤销（且撤销在投递时刻之前）被如实记账
  ✓ roundBase：没有新登记时，旧条目的轮次身份原样保留（不能被改写成当前轮）
  ✓ 截止台账：delivered 终态保留
  ✓ 截止台账：原生确认撤销 → cancelled（不谎报送达、也不补发）
  ✓ 截止台账：delivered / cancelled 两类记录都还在（没有借性能改动清除旧证据）

== G. O6：无显示变化的一拍不重写容器；时间 / 数量 / 展开状态变了必须重建 ==
  ✓ 首拍：真的渲染出了卡片，且文案是「1 小时后」
  ✓ 无显示变化的一拍：#homeDue **一次都没被重写**（不是「重写了相同内容」）
  ✓ 无显示变化的一拍：记为 skipped，builds 不增
  ✓ 无显示变化的一拍：容器内容逐字节相同 ⇒ 卡片节点没有被替换（焦点/滚动/忙碌态因此不受影响）
  ✓ 跨日文案变化：容器被重建（不是只有 rev 变了才更新）
  ✓ 重建后的文案确实换了（1 小时后 → 3 天后）
  ✓ 新增一条到期事项 → 重建，并且卡片数是 2
  ✓ 折叠态：入口只有数量，没有卡片
  ✓ 展开状态变化 ⇒ 重建（展开后卡片出现）
  ✓ #homeReview 连续两拍无变化 ⇒ 容器不重写（它本来每拍都会被调一次）
  ✓ #homeReview 数量真的变了 ⇒ 立刻重建（不滞后）
  ✓ 启动期 #homeNotice 只被写 1 次（原先每读一次原生状态就重写一次同一段内容）
  ✓ 启动期 #homeSetup 只被写 1 次
  ✓ 总开关默认关闭 ⇒ 告知条给的是「后台提醒已关闭」，不是权限话术（Q6 的既有结论）
  ✓ 优先级：总开关关闭 + 权限被拒 → 说的是「开关」，不是「去授权」
  ✓ 优先级：总开关打开 + 权限被拒 → 才是权限话术
  ✓ 链路正常（权限给了、对账没报错）→ 没有告知条
  ✓ 结论**没变**（仍被总开关话术遮住）⇒ 容器不重写，也没被误升级成权限警告
  ✓ 结论真的变了 ⇒ 立刻更新（不滞后）
  ✓ 同一结论重复到达 3 次 ⇒ 容器一次都没被重写（内容相同就不重建）
  ✓ 断链消失 ⇒ 告知条被清空（权限恢复后警告不残留）
  ✓ 清空之后再次到达同一结论 ⇒ 仍然不重写

== G2. O6：首页签名保留数组与字段边界，用户内容不能制造碰撞 ==
  ✓ ["work,home"] → ["work", "home"]：无需重载就重建为两个标签
  ✓ 字段中出现旧 SEP 字符也不会把 title/note 边界混在一起
  ✓ 项目字段中出现旧 ROW 字符也不会把相邻项目边界混在一起
  ✓ 结构化签名在内容不变时仍保持短路，不牺牲原优化

== H. O7：深链 / 分享入口不为满足「1 次」而丢功能，也不先渲染一次首页再切走 ==
  ✓ 深链 ?tab=future：ready 且路由生效
  ✓ 深链 ?tab=future：futureSeg 被落到 waiting（保留原语义）
  ✓ 深链 ?tab=future：首页三个卡片容器一次都没被渲染（不是「先渲染首页再切走」）
  ✓ 深链 ?tab=future：末尾 tick 仍然执行（心跳没被路由分支吞掉）
  ✓ 分享参数：ready 且被路由回首页
  ✓ 分享参数：标题确实进了表单（捕获发生在初始化里，不是被渲染短路掉）
  ✓ 分享参数：首页容器各只写 1 次（捕获与渲染合并成一次）

========== 生产组合启动测试结果 ==========
通过: 436  失败: 0
全部通过。
