# NOT_PERFORMED

- `HOME + am kill` 后进程仍存活，后台回收场景未构造成功；保留 pid 与 AMS 绑定取证。
- 未修改候选 APK 进行 WebView JavaScript 层故障注入。
- 未在真机破坏或清空 localStorage 镜像。
- 未实施导入格式安全面。
- 未执行 Git checkout、stash、reset、commit 或 push。
