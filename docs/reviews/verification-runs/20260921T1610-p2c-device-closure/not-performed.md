# 本 run 未执行 / 未成立项（NOT_PERFORMED）

> 纪律：**「报告说已验证」≠「验证成立」**。以下条目未在真机上成立，逐条给出原因与替代证据，不得折算为 PASS。

## 1. 宽限闸门 8 秒超时分支（结果永不返回）

- 目标行为：`noteExportAppVisibility` 中 8 s 定时器到期 ⇒ 作废操作身份、恢复按钮、toast「保存结果未返回 · 文件状态无法确认，请重试」。
- 强制构造：导出中 → 按 HOME（`vc=hidden`，置 `leftApp=true`）→ `am force-stop com.coloros.filemanager`（选择器进程被杀）→ 回前台（`vc=visible`+`resume`，8 s 计时开始）。
- 结果：平台**仍把 RESULT_CANCELED 投递给应用**，toast 为「已取消导出 · 未保存文件」 ⇒ 定时器守卫 `pendingNativeExport !== pending` 生效并正确早退，**超时分支未被触达**。
- 已成立的相关面：闸门**输入路径真机可达**（`vc=hidden`/`vc=visible` 确实触发），且真结果到达时**不误报**超时提示。
- 替代证据：源码分支存在（`lib/app-backup.js:196-204`）；`test-smoke.js:1547-1548` 走了 `noteExportAppVisibility(false)/(true)` 的非超时路径。
- **附带缺口（已由 P2-C-R 补齐，待独立复验）**：本 run 结束时，全仓测试**没有任何断言覆盖 8 s 超时分支**
  （全仓 grep `保存结果未返回` 只命中源码与历史副本，命中不了任何测试文件）。
  该缺口已在 `20260921T2355-p2cr-authority-gate` 一批中补上假计时器断言（7999 ms 不触发 / 8000 ms 触发并恢复按钮 /
  超时后迟到结果不再 toast / 新一轮导出不受上一轮迟到结果影响 / 删掉超时守卫后反例必红）。

## 2. 导出「unavailable」分支（运行时不具备系统保存能力）

- 目标行为：`isNativeAndroidRuntime()` 为真但 `systemBridge().saveDocument` 缺失 ⇒ toast「当前版本缺少系统保存能力 · 文件未导出，请更新应用后重试」。
- 未执行原因：真机运行的正是原生 Android 通道，**无法在不改包的前提下摘掉桥**；改包即失去本 run 的等价性前提。
- 替代证据：Chrome 用例 `missing-app-backup` / `shell-backup`（12 用例 0 失败，见 `20260921T1330…` / `20260921T1425…` run）。

## 3. 导入「读取失败」反馈分支（G01：`FileReader.onerror/onabort`）

- 目标行为：文件读取失败/中止 ⇒ 止步读取层提示，不弹确认框、不触发提交。
- 未执行原因：真机上无法在「选中文件」与「读取」之间制造稳定的读取失败（需让已在选择器中确认的文件在读取前失效，不可控且会引入竞态）。
- 替代证据：`test-unit.js` 新增 8 条（假 `FileReader` × `save` 三态），含 `onerror/onabort` 断言；变异取证已证明「拔掉修复必变红」。

## 4. 「迟到结果被丢弃」（`stale`）时序

- 未执行原因：触达前提是**先发生宽限超时作废**，而第 1 条已证明该前提在真机不可达。
- 替代证据：源码 `operationId !== exportOperationId ⇒ return {status:"stale"}`（`lib/app-backup.js:244`）；无自动化断言（同第 1 条缺口）。

## 5. E1–E4 与「导出没有落地出口」的准确口径（**文义校正**）

- **已经成立的**：本 run 用的是当前隔离候选的**真实设备**，`<a download>` 尚存的问题**不影响**这几条 ——
  - 导出**落盘**（`/sdcard/安心收件箱备份-2026-09-21.json`，真实文件字节可取回）；
  - **同名不覆盖**（第二次导出落到 `_1.json`，且 toast 回填的是**系统给的真实文件名**）；
  - **取消**（返回后界面/按钮/副文案完整还原）；
  - **导入回环**（选中文件 → 确认框 → 1 项目/1 事项恢复）。
  这四条正是 **E1–E4 提案所指向的主要行为**，故不再记作「E1–E4 未执行」。
- **仍未成立的只有 `<a download>` 这条通路本身**：真机走的是原生 SAF 通道，**不经过** Web `<a download>`；
  该通路「无 Android 接应、无条件弹成功」的问题**逐字保留为已知开口缺陷**，实现未动，去留待裁决 E1–E4。

## 6. 本轮未覆盖的其他面（逐项列清）

- **未执行（保留 NOT_PERFORMED）**：
  1. **正式生产包** `space.alliswell.inbox` 上的安装与验收（本 run 全程只用隔离包 `…exportrecheck`）；
  2. 宽限闸门 **8 秒真实超时**（见 §1）；
  3. 导出 **unavailable**（见 §2）；
  4. **stale** 迟到结果时序（见 §4）；
  5. **G01 真机故障注入**（见 §3）。
- **未覆盖（属其他批次）**：导入的 schema 版本 / app 身份 / 字段约束 / 未知字段处理 / 整体拒绝零副作用 ——
  属**待单独裁决的导入格式安全面**（5 步计划第 ② 步），本 run 只覆盖「非法 JSON 全量拒绝且零副作用」一条。
- **未做**：P3 / P4、正式打包链 cmp（本 run 只对候选 APK 做过 APK 内资源 ↔ 源码逐字节比对）。
