# 观察项：进程死亡后 IDB 状态内容丢失（机制未确立）

- 时间线（全部有物证）：
  - 16:16 设备导出 `_1.json` 成功，内容 1 项目/1 事项（"P2C 复验事项"），当时 IDB 权威通道 = 1/1（导出数据来源于 IDB 读出后的 state）。
  - ~16:2x 设备被用户拿起；此后数据线断开、进程死亡（23:07 重连后 pid 由 6962 变 30370）。
  - 23:12 直读 IDB（`attention-inbox`/kv/`state`）：**schema=5，projects=0，items=0**。
- 结论：IDB 内容从 1/1 变 0/0，发生在上述区间内，**机制未确立（NOT_ESTABLISHED）**。
- 候选假设（均未证实，不下结论）：
  1. 冷启动 loadAsync 与某处 save() 竞态：空内存状态在加载完成前/失败后被提交，覆盖 IDB（若成立属数据丢失级缺陷）。
  2. 状态加载失败被静默吞掉后仍继续运行并保存。
  3. 其他未知路径。
- 缓解事实：`_1.json` 完整快照在设备 `/sdcard/` 与本 run 目录 device-exports/ 各有一份，数据未真正丢失。
- 后续处置建议：单独裁决批次（导入格式安全面）之后，把「冷启动加载失败/竞态不得覆盖既有 IDB」列入待裁决；本轮不就地修复。
- 关联：本 run 的导入回环改为「从空状态导入 `_1.json` → 验证恢复 1/1」，恰好覆盖导入主路径。
