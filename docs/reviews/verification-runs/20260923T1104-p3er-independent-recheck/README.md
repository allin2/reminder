# P3-E-R independent recheck: source PASS, delivery FIX_REQUIRED

This run independently checks the repaired P3-E workspace in `/Users/qlyf/Developer/reminder`. Branch `main`; HEAD and `origin/main` are `3574824357dc7beb04cbd3e32aa413cd508e8484`. No product source, APK, old evidence, or user data was edited by this run. Exact current hashes are in `source-hashes.txt`.

## The two previously blocking source defects are closed

- `lib/app-native-coordinator.js` now requires `handleAlarmAction` and passes it directly to `NativeReminders.drainAlarmActions`; `app-core.js` provides the transaction callback. The notification callback remains separate. Independent `verify-two-fixes.js` confirms `close` and `ack` reach the direct callback with their original event IDs, and an in-memory wrong-route mutation sends both to the notification callback instead. `verify-production-route.js` drives the actual production combination through `fireResume()`: close does not open detail; ACK becomes authoritative and its event ID survives cold boot. Both probes exit 0.
- The reconcile loop now exits only after a version-stable run. The independent probe bumps the version once while `reconcile` is active: exactly two runs occur and the second receives the new item snapshot. An in-memory mutant with the old premature break runs only once. Exit 0.
- Fresh `npm test` exits 0: unit 642, native 324, boot 782, smoke 266, regressions 730, parse 160. Fresh isolated Chrome recovery checks pass 12/12. `node --check` on changed JS and `git diff --check` pass. The 32 Web resources match byte-for-byte across source, `www`, Android assets, debug intermediate assets, and the actual Debug APK; APK SHA-256 is `96a2a0a41e8ba55652297fd94d6f9b2560e8801e1ff4d4b38b6979b6698d8300`.

## Delivery blockers

1. **Previously named candidate path was overwritten.** The first P3-E run recorded `releases/candidates/20260923T1000-p3e-candidate/app-debug.apk` as SHA-256 `ebc57bb6c8423334cd9f8b717130433ee6edcc8d0ac2ab80aa766a5e7c53754f` (`20260923T1000-p3e-app-native-coordinator/debug-apk-sha256.txt`). It now contains `96a2a0a4…`. The original `ebc57bb6…` bytes remain at `releases/candidates/20260923T0958-p3e-candidate/app-debug.apk`, so they are recoverable, but the `1000` candidate's recorded path-to-hash identity was broken. The repair report's claim that all existing candidates were preserved is false for that named path. Do not relabel either prior run. Freeze the repaired APK under a new, unique candidate directory and document both historical identities and the overwritten alias.
2. **PWA cache version was not advanced after changing runtime scripts.** `app-core.js` and `lib/app-native-coordinator.js` changed from the P3-E v29 build, but `sw.js` is byte-identical (`1042baed…`) and still declares `attention-inbox-v29`. The repository's Service Worker contract explicitly requires a new cache name when a runtime script's content changes; its pre-cache is keyed by URL. The package resource check proves internal consistency of the new APK, not upgrade behavior for an already installed PWA. Advance the cache version for this repair, rebuild into the new candidate path, and rerun the resource comparison. Physical offline upgrade remains a later P4 test and must remain NOT_PERFORMED here.

The repair README also lists incorrect pre-repair source hashes for `app-core.js` and `lib/app-native-coordinator.js`; correct these using the archived `20260923T1014-p3e-independent-recheck/source-hashes.txt` and keep the original inaccurate report as historical evidence.

The result is **PASS for the two repaired source behaviors** and **FIX_REQUIRED for delivery identity and cache version**. Do not start the next modularization batch or call this candidate fully accepted until a new immutable candidate and current SW version are independently checked. Physical Android installation/native visible delivery and real offline Service Worker v28→v29→next upgrade are **NOT_PERFORMED**.
